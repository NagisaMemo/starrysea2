spring: 
  application:
    name: starrysea-order
  cloud:
    config:
      profile: dev
      label: null
      discovery:
        enabled: true
        service-id: starrysea-config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
  instance:
    prefer-ip-address: true